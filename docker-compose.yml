version: "3.9"

services:
  api:
    build: .
    container_name: fastapi-book-catalog
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn project.app:app --reload --host 0.0.0.0 --port 8000
